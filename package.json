{
  "name": "bighorn-threads",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "gsc:submit": "node scripts/gsc-submit.mjs",
    "deploy:pages": "npm run build && npx wrangler pages deploy dist --project-name=bighorn-threads --branch=master",
    "deploy": "npm run deploy:pages && npm run gsc:submit"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/typography": "^0.5.20",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.1.6",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "nodemailer": "^9.0.4",
    "typescript": "^5.9.3"
  }
}
